var wxBridge = require('./util/wxjsbridge.js');
var index = {
    init:function(){
        //wxBridge.setTitle("自定义title");
        //wxBridge.refresh();
    }
};
$(document).ready(function(){
    index.init();
});
